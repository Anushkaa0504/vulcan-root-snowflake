semantic_models:
  ECOMMERCE_PLATFORM.mart.fct_events:
    alias: fct_events

    dimensions:
      includes:
        - EVENT_DATE
        - ITEM_ID

    measures:
      total_events:
        expression: "{fct_events.TOTAL_EVENTS}"
        type: sum

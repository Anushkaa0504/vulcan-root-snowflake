checks:
  staging.stg_orders:

    accuracy:
      - row_count > 1:
          name: sufficient_data

      - anomaly detection for row_count:
          name: row_count_anomaly

    completeness:
      - missing_percent(id) < 5:
          name: id_mostly_present